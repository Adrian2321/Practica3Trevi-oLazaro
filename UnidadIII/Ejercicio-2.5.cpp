#include "iostream"
using namespace std;

int main()
{
	cout<<"Hola! Este programa 5 unidad 2"<<"\n";
	int num;
    cout<<"Escribe un numero: "<<"\n";
    cin>>num;
	
	if (num% 2 == 0) cout<<"El numero es par ";
	  
	  else
	  
	  if(num% 3==0) cout<<"El numero es impar y no es divisible por 3";
	  
	       else cout<<"El Numero es impar, no es divisible por 3";
	
}
